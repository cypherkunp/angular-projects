import { NgFalseDirective } from './ng-false.directive';

describe('NgFalseDirective', () => {
  it('should create an instance', () => {
    const directive = new NgFalseDirective();
    expect(directive).toBeTruthy();
  });
});
