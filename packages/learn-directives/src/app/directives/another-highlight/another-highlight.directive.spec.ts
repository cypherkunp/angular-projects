import { AnotherHighlightDirective } from './another-highlight.directive';

describe('AnotherHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new AnotherHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
